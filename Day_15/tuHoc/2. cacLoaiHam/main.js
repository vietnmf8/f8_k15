/* Declaration Function: */
function tenHam(thamSo) {
    // Khối lệnh
}

/* Function Expression - Gán hàm vào biến */
const tenHam = function (thamSo) {
    // Khối lệnh
};

/* Arrow Function */
const tenHam = (thamSo) => {
    // Khối lệnh
};

const hamCong = (thamSo1, thamSo2) => thamSo1 + thamSo2; // Khối lệnh

const hamNhan = (a) => a * a;
